# Requirements for the Lead Recovery Dashboard
# Assuming Python >= 3.10 is already installed

# Core dashboard library
streamlit>=1.33

# Data manipulation (might already be in base env)
pandas>=1.5 # Specify a reasonable minimum version

# Required by Streamlit for date/time handling and potentially dataframes
pyarrow>=10.0

# Charting library
altair>=5.0

# Optional: If st.dataframe doesn't suffice and AgGrid is needed later
# streamlit-aggrid>=0.3.4 # Uncomment if switching to AgGrid 